{
  "name": "input_inject_requires_pie_error",
  "description": "Input injection is refused with a 409 while no PIE session is running.",
  "route": "/api/input/inject",
  "tool": "input_inject",
  "request": {
    "operation": "press_key",
    "key": "W"
  },
  "status": 409,
  "response": {
    "ok": false,
    "error": {
      "code": "pie_not_running",
      "message": "input injection requires a running PIE session with a player controller — start one with pie_control operation 'start'"
    }
  }
}
